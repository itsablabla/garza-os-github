{
  "name": "replicas-matrix-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that bridges a Matrix bot to Replicas workspaces (Phase 1 — unencrypted rooms, sibling to replicas-telegram-bridge)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.5",
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "@matrix-org/olm": "^3.2.15"
  }
}
