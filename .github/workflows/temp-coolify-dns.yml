name: Temp coolify wildcard DNS
on:
  workflow_dispatch:
  push:
    paths:
      - "scripts/upsert-coolify-dns.sh"
      - ".github/workflows/temp-coolify-dns.yml"
jobs:
  dns:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Upsert DNS
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: bash scripts/upsert-coolify-dns.sh
